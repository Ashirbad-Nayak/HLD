/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['https://lh3.googleusercontent.com/'], // ← replace with actual domain
      },
};

export default nextConfig;
